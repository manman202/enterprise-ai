# Lightweight requirements for CI test environment.
# Excludes sentence-transformers (pulls torch ~3 GB) and other heavy deps
# that are not needed to run the test suite. Production image uses requirements.txt.

fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
websockets==13.1

pydantic-settings==2.7.0
email-validator==2.2.0

sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

httpx==0.28.1

PyJWT==2.10.1
bcrypt==4.3.0
ldap3==2.9.1

pypdf==4.3.1
python-docx==1.1.2
openpyxl==3.1.5
aiofiles==24.1.0
watchdog==6.0.0

# sentence-transformers intentionally excluded — mocked in conftest.py
