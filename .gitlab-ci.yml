stages:
  - lint
  - test
  - build
  - deploy-dev
  - deploy-prod
  - mirror

variables:
  DOCKER_DRIVER: overlay2
  DOCKER_TLS_CERTDIR: ""

# ─── LINT ─────────────────────────────────────────────────────
lint:backend:
  stage: lint
  image: python:3.12-slim
  script:
    - pip install flake8 --quiet
    - cd backend
    - echo "→ Backend lint OK (no files yet — placeholder)"
  allow_failure: true

lint:frontend:
  stage: lint
  image: node:20-alpine
  script:
    - echo "→ Frontend lint OK (no files yet — placeholder)"
  allow_failure: true

# ─── TEST ─────────────────────────────────────────────────────
test:backend:
  stage: test
  image: python:3.12-slim
  script:
    - echo "→ Backend tests OK (placeholder)"
  allow_failure: true

# ─── MIRROR TO GITHUB ─────────────────────────────────────────
mirror:github:
  stage: mirror
  image: alpine/git:latest
  script:
    - echo "→ Mirroring to GitHub..."
    - git remote add github https://manman202:${GITHUB_TOKEN}@github.com/manman202/enterprise-ai.git 2>/dev/null || true
    - git push github HEAD:$CI_COMMIT_BRANCH --force
    - echo "✓ Mirror to GitHub complete"
  rules:
    - if: $CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop"
  allow_failure: true
